import { LANDMARKS } from "../server/data/landmarks";
import { CITIES } from "../server/data/cities";

console.log(`Hardcoded LANDMARKS count: ${LANDMARKS.length}`);
console.log(`Hardcoded CITIES count: ${CITIES.length}`);
