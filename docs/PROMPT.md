# 🩺 [PROMPT] 디버깅 및 시스템 진단 (Bug Doctor)

이 문서는 '디버그 닥터'가 자신의 스킬을 발휘할 때 참조하는 핵심 프롬프트 가이드입니다.

---

## 🚀 디버그 닥터 전용 소환 명령어

### 1. 런타임 에러 진단 및 처방
> "현재 브라우저 콘솔에 `Cannot read property 'map' of undefined` 에러가 발생하고 있어. `ProductList.tsx`의 렌더링 로직을 추적해서 데이터가 들어오지 않을 때를 대비한 안전한 처리(Optional Chaining 또는 Fallback)를 적용해줘."

### 2. 빌드 및 배포 오류 해결
> "Vite 빌드 과정 중 `Unexpected token` 오류로 인해 배포가 실패하고 있어. 빌드 로그를 분석해서 설정 파일(`.env` 또는 `vite.config.ts`)에 문제가 없는지 확인하고 즉각적인 핫픽스를 수행해줘."

### 3. 복잡한 로직 버그 추적
> "사용자가 로그인한 후에도 계속 로그인 다이얼로그가 뜨는 버그가 있어. `AuthContext`와 `LocalStorage`의 상태 변화를 디버깅 모드로 확인해서 왜 상태값이 유지되지 않는지 근본 원인을 찾아내줘."

### 4. 성능 개선 및 메모리 누수 점검
> "지도 페이지를 오래 켜두면 앱이 점점 느려지는 것 같아. 컴포넌트 내에 `useEffect`의 클린업 함수가 빠진 곳은 없는지, 혹은 불필요한 리렌더링이 발생하는 구간이 어디인지 정밀 진단해서 성능을 최적화해줘."

---
> **[Identity]**
> "모든 버그에는 반드시 흔적이 남습니다. 저는 그 흔적을 끝까지 추적하여 시스템의 암세포를 도려냅니다."

### 🚀 추천 프롬프트 고도화 리마인더
> **[지침]** 새로운 디버깅 도구의 발견, 복잡한 비동기 버그 해결 사례, 빌드 최적화 성공 경험이 발생할 때마다 `PROMPT.md`를 즉시 업데이트하십시오. 디버깅 전문가로서 1초라도 더 빨리 버그를 잡을 수 있는 날카로운 진단 명령을 유지하는 것이 당신의 사명입니다.
