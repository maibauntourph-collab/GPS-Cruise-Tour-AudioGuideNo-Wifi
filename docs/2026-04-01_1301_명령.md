# 2026-04-01 13:01 Viator Sandbox API Proxy 전환

## 1. 목적 (Goal)
- 사용자가 "Booking for Viator" 버튼 클릭 시, 이전처럼 Viator 검색 페이지가 아닌 `https://gps-audio-guide-no-wifi.maibauntourph.workers.dev/` Cloudflare Worker 프록시(Proxy)를 경유하여 `https://api.sandbox.viator.com/partner/products/modified-since?count=500` API를 호출(GET) 하도록 연결 URL 변경.

## 2. 작업 내역 (Tasks completed)
- **`client/src/lib/affiliateConfig.ts` 수정**: 
  - `getViatorUrl` 함수가 `proxy` + `targetApi` 조합의 URL을 반환하도록 로직을 교체했습니다.
  - 이를 통해 사용자가 Viator 예약 버튼을 클릭하면 브라우저 새 탭에서 API로부터 JSON 데이터를 가져오는(GET) 동작이 대리(Proxy) 수행됩니다.
- **History 기록**: 
  - `History-workflow-book.md` 파일에 이번 변경 작업을 Append.

## 3. 추천 프롬프트 및 추천 에이전트 (@에이)
- **추천 에이전트**: `Server Park` (백엔드 및 API 코디네이터) 
- **추천 프롬프트**: "add, com, dep 를 통해서 방금 변경한 Viator 프록시 URL 적용사항을 저장하고 클라우드에 배포해줘."

*작성 일시: 2026-04-01 13:01:25*
