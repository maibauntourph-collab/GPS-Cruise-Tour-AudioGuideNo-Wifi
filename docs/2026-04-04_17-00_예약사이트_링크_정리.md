# 🔗 제휴 예약 사이트 링크 및 검색 로직 정리

본 문서는 GPS Cruise Tour Audio Guide 앱에서 사용하는 주요 제휴 예약 플랫폼의 검색 링크 생성 및 연동 방식을 정리한 가이드입니다. 

---

## 1. 개요 (Overview)
사용자가 명소 상세 정보 화면에서 '티켓/예약' 탭을 클릭했을 때, 각 플랫폼으로 즉시 연결되는 퀵 링크(Quick Links)를 제공합니다. 모든 링크는 **새 창(`_blank`)**으로 열리며, 우리 앱은 백그라운드에서 계속 유지(**Stay**)됩니다.

## 2. 제휴 플랫폼 목록 및 검색 로직

| 플랫폼 | 아이콘 | 주요 지역/특징 | 검색 파라미터 |
| :--- | :---: | :--- | :--- |
| **Booking.com** | 🏨 | 전 세계 숙소 및 액티비티 | `ss={검색어}&aid={ID}` |
| **Agoda** | 🏨 | 아시아권 숙소 특화 | `keyword={검색어}` |
| **Klook** | 📱 | 아시아/액티비티 특화 | `keyword={검색어}&aid={ID}` |
| **GetYourGuide** | ✅ | 유럽 투어/티켓 특화 | `q={검색어}&partner_id={ID}` |
| **Viator** | 🌍 | 미주/유럽 글로벌 투어 | `/search/{검색어}?text={검색어}&pid={ID}` |
| **Trip.com** | 💰 | 중화권 및 항공/호텔 | `query={검색어}&allianceid={ID}` |
| **MyRealTrip** | 🇰🇷 | 한국인 맞춤 투어/티켓 | `q={검색어}&partner_id={ID}` |

## 3. 핵심 구현 코드 (Technical Summary)

### 3.1 URL 생성 함수 (`affiliateConfig.ts`)
각 플랫폼의 최신 검색 URL 스펙에 맞춰 검색어와 제휴 ID를 조합합니다.
- **검색어 최적화**: 글로벌 플랫폼의 경우, 가능한 명소의 **영어 이름(English Name)**을 우선적으로 사용하여 검색 결과의 풍부함을 보장합니다.

### 3.2 팝업 차단 방지 및 앱 유지 (`LandmarkDetailDialog.tsx`)
브라우저의 팝업 차단을 피하기 위해 `window.open`을 클릭 이벤트 핸들러 내부에서 **동기적**으로 실행합니다.

```typescript
const openExternalUrl = (url: string, target: string = '_blank') => {
  // 새 탭 오픈 (Noopener 설정으로 보안 강화)
  const win = window.open(url, target, 'noopener');
  
  // 팝업 차단 발생 시 폴백 처리
  if (!win) {
    window.location.href = url;
  }
  return win;
};
```

## 4. 사용자 경험 (UX) 포인트
- **App Stays Open**: 외부 사이트로 이동해도 사용자가 앱으로 돌아왔을 때 현재 보고 있는 명소가 그대로 유지됩니다.
- **Language Localization**: 아고다(Agoda)나 클룩(Klook) 등은 사용자의 선택 언어에 따라 자동으로 현지화된 페이지(`ko-kr`, `en-us` 등)로 연결됩니다.

---
> [!NOTE]
> 본 정보는 2026년 4월 4일 기준으로 최신화되었습니다. 제휴 플랫폼의 URL 스펙 변경 시 `affiliateConfig.ts`를 업데이트하십시오.
