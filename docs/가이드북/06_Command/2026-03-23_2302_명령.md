# 명령.md — 2026-03-23 23:02

## ✅ 작업 완료: Country & City Selection (② city select 화면 구현)

### ⏰ 작업 시간: 2026-03-23 23:00 ~ 23:02 KST

---

## 📋 요청 사항
- `country and city selection like pic` — 이미지 ② city select 화면과 동일한 UI 구현

---

## 🔧 수정 파일 목록

| 파일 | 변경 내용 |
|------|-----------|
| `client/src/components/CitySelectTab.tsx` | 전면 재설계 — 이미지와 1:1 구현 |
| `client/src/pages/Home.tsx` | CitySelectTab import 추가, full-screen 모드 연동, 구 코드 제거 |

---

## 🎨 구현 내용 (이미지 ② city select)

### 1. PWA 설치 배너 (상단 다크 배경)
- `#242424` 다크 박스 + 오렌지 다운로드 아이콘
- "홈화면에 추가하면 **완전 오프라인!**" 문구
- 25개 언어 다국어 지원
- [설치] 버튼

### 2. 헤더 섹션
- **기항지 가이드** 타이틀 (다국어)
- **한 / EN / 中** 언어 토글 버튼 (오렌지 활성)

### 3. 카테고리 필터 탭
- **전체 / 아시아 / 유럽 / 크루즈 추천** (수평 스크롤)
- 활성 탭: 오렌지 배경

### 4. 도시 카드 목록 (수직 스크롤)
- 상단: 파스텔 배경 + 이미지 오버레이 + 도시명 대문자
- 자물쇠 아이콘 (idx > 0 잠김)
- 중문 표시 뱃지 (zh 언어일 때)
- 하단: 도시명(현지어+국가) + 통계 문구

### 5. 바텀 내비게이션 바 🆕
- **🏠 도시 / 🗺️ 지도 / 👤 내 플랜** 탭
- 활성 탭: 오렌지 색상 + 점 표시
- 지도 탭 → showCountrySelector 닫고 map 모드
- 내 플랜 탭 → list 모드

### 6. 전환 애니메이션
- 기존 바텀시트 (y축 슬라이드) → **x축 슬라이드** (네이티브 앱 느낌)
- `spring` 애니메이션 (damping 30, stiffness 300)

---

## 🐛 수정된 버그
- 기존 `false &&` 사문화 코드 제거
- 중복 AnimatePresence 블록 정리

---

## 📡 서버 상태
- Port 5000 이미 실행 중 (`EADDRINUSE`) → 기존 서버 활성
- 브라우저 도구 연결 오류 (CDP 9222) — 환경 문제

---

## ✅ 에이전트 보고

| 항목 | 내용 |
|------|------|
| 에이전트 | Designer Kim (UI/UX) + Bug Doctor (코드 정리) |
| MCP | 없음 |
| 사용 토큰 | ~중간 규모 |
| 스킬 | `designer_kim`, `bug_doctor` |

---

> 🎓 **교수님 노트**: 이번 작업의 핵심 포인트는 "컴포넌트의 단일 책임 원칙(SRP)"입니다.
> `CitySelectTab`이 자체 바텀 내비게이션까지 포함하여 도시 선택 화면 전체를 담당하고,
> `Home.tsx`는 이 컴포넌트를 언제/어떻게 보여줄지만 결정합니다. 역할 분리가 명확하죠!
