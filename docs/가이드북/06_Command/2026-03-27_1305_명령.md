# 오디오 가이드 시작 시 재생 메커니즘 분석 및 응답

## 사용자 요청
"WHEN I PLAY START AUDIO, WHAT IS PLAYING?"

## 안티그라비티 추천 에이전트 및 프롬프트

### 1. 추천 에이전트 및 스킬
- **에이전트**: Bug Doctor (AI 프로그램 디버깅 전문가) & Story Teller Lee (스토리텔링 마스터)
- **MCP**: TestSprite (기능 테스트 및 오류 검증)
- **Skill**: `bug_doctor` (코드 동작 분석) 및 `story_teller_lee` (콘텐츠 전달)

### 2. 추천 프롬프트
*"사용자가 명소 상세 페이지에서 'Start Audio' 버튼을 눌렀을 때 재생되는 텍스트의 출처와 번역 및 오디오 엔진(TTS/OpenAI) 처리 과정을 분석하여, 어떠한 콘텐츠가 재생되는지 설명해 주세요."*

---

> **교수님 승인을 대기 중입니다.**  
> (위 추천안과 아래의 상세 설명을 확인하시고, 추가 수정이나 코드 변경이 필요하시면 말씀해 주십시오.)

---

## 👨‍🏫 교수님의 친절한 설명

학생 여러분, 사용자가 명소 화면에서 **"Start Audio(오디오 시작)"** 버튼을 클릭할 때 어떤 일이 발생하는지 알아봅시다.

이 과정은 `client/src/components/LandmarkDetailDialog.tsx` 파일 내의 `handlePlayAudio` 함수에서 시작됩니다.

### 1단계: 무엇을 읽어줄 것인가? (텍스트 결정)
버튼을 누르면 시스템은 가장 먼저 "어떤 내용을 읽어줄지" 결정합니다.
우선순위는 다음과 같습니다:
1. **나레이션 가이드 정보 (`detailedDescription`)**: 만약 학생이 특정 가이드를 선택했다면, 해당 가이드의 상세 설명을 가져옵니다.
2. **명소의 상세 설명 (`translatedDetail`)**: 기본적으로 명소에 등록된 가장 상세한 텍스트를 읽습니다.
3. **요약 나레이션 (`translatedNarration`)**: 상세 설명이 없으면 요약된 나레이션 텍스트를 찾습니다.
4. **기본 설명 (`translatedDesc`)**: 위 항목들이 모두 비어 있다면, 명소의 가장 짧은 기본 설명을 읽어줍니다.

*즉, 페이지에 등록된 정보 중 가능하면 가장 풍부하고 상세한(Detailed Description) 정보를 가져와서 읽어주려고 노력하는 구조랍니다.*

### 2단계: 어떤 언어로 읽어줄 것인가? (언어 감지)
오프라인 환경(No-WiFi)에서는 가끔 번역이 실패해 원본 영어 텍스트가 그대로 남아있는 경우가 있습니다. 
만약 화면은 '한국어'인데 텍스트는 '영어'일 때, 한국어 음성으로 영어를 읽으면 매우 어색하겠죠?
그래서 `AudioService.resolvePlaybackLanguage` 함수가 실제 텍스트의 유니코드(글자 모양)를 스캔해서 텍스트의 진짜 언어를 감지하고, 그 언어에 맞는 발음 기호를 선택합니다.

### 3단계: 어떻게 읽어줄 것인가? (오디오 엔진 선택)
텍스트와 언어가 결정되면, 드디어 재생 엔진을 거치게 됩니다.
1. **OpenAI 모드 (`audioMode === 'openai'`)**: 고품질의 OpenAI 음성 서비스가 활성화되어 있다면 `playOpenAISentences`를 사용하여 읽습니다.
2. **원시 TTS 방식 (`auto` 또는 `tts`)**: 오프라인 상태이거나 OpenAI 호출에 실패하면 모바일 브라우저에 내장된 웹 스피치 엔진(`speechSynthesis`)이 문장 단위로 분리(`splitIntoSentences`)된 텍스트를 차례대로 읽어줍니다. 이때 화면에는 현재 읽고 있는 문장이 노란색 형광펜(`bg-[#ccff00]`)으로 칠해지는 효과가 나타납니다.

결론적으로, "Start Audio"를 누르면 **현재 선택된 명소(또는 가이드)의 가장 상세한 역사적/문화적 설명문**이 실시간 번역된 상태로 문장 단위로 재생됩니다!
