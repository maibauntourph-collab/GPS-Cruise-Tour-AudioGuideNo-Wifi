큰 문제 발견! narrationI18n에 이전의 짧은 영어 텍스트가 남아있어서 TTS가 새 나레이션을 무시하고 있습니다.

분석 결과

나레이션 해석 우선순위:
  1순위: narrationI18n[lang]     ← 여기에 옛날 짧은 영어(19~37ch)가 들어있음!
  2순위: translations[lang]      ← 여기도 같은 짧은 텍스트
  3순위: narration 필드          ← 우리가 업데이트한 새 한국어(600+ch) ✅
문제: narrationI18n.en에 이전 짧은 영어 슬로건(예: "K-Beauty sanctuary.")이 1순위로 남아있어서, 모든 언어의 TTS가 이 짧은 텍스트를 읽고 있습니다. 새로 넣은 한국어 나레이션(600+자)은 3순위 fallback이라 TTS에 도달하지 못합니다.

narrationI18n에 한국어(ko) 나레이션을 추가해야 합니다. 수정할까요?

e:\GPS-Cruise-Tour-AudioGuideNo-Wifi-1\readme.md
move md file to /docs/,
Bash Check docs directory contents
IN
ls docs/ 2>/dev/null | head -20

OUT
2015-01-30.md
2026-02-21_2140_progress.md
2026-02-22_0325_progress.md
2026-02-22_0640_whitescreen_fix.md
2026-02-23_0050_implementation_plan.md
2026-02-23_0050_task.md
2026-02-23_0050_walkthrough.md
2026-02-23_1855_walkthrough_gemini_role.md
2026-02-24_1701_명령.md
2026-02-24_1705_오디오_시스템_분석_보고서.md
2026-02-24_1705_오디오_코드_주석_가이드.md
2026-02-24_1835_명령.md
2026-02-24_1840_명령.md
2026-02-24_1950_명령.md
2026-02-24_2005_명령.md
2026-02-25_0358_명령.md
2026-02-25_0440_명령.md
2026-02-26_0550_작업완료보고서.md
2026-02-26_1919_landing_shopping_plan.md
2026-03-17_2203_명령.md
Bash Move readme.md to docs/
IN
mv readme.md docs/readme.md

OUT
(Bash completed with no output)
Done. readme.md moved to docs/.

나레이션 I18n 수정도 진행할까요? (narrationI18n.ko에 새 나레이션 넣어야 TTS 매칭됨)

나레이션 해석 우선순위:
  1순위: narrationI18n[lang]     ← 여기에 옛날 짧은 영어(19~37ch)가 들어있음!
  2순위: translations[lang]      ← 여기도 같은 짧은 텍스트
  3순위: narration 필드          ← 우리가 업데이트한 새 한국어(600+ch) ✅
이것을 전체적으로 마이그레이션이  필요해 어떻게 진행하는 것이 좋을까
@advisor 참여해서 전략을 만들어 보자
Agent:Narration I18n migration strategy
IN
## Context

We have a GPS Cruise Tour Audio Guide app with 249 landmarks in a NeonDB (PostgreSQL) database. The narration system has a priority resolution bug affecting TTS playback.

### Current Schema (Drizzle ORM)
```typescript
// In landmarks table:
narration: text("narration").notNull(),           // Base narration text
narrationI18n: json("narration_i18n"),             // Record<string, string> e.g. {"ko": "...", "en": "..."}
descriptionI18n: json("description_i18n"),         // Record<string, string>
translations: json("translations"),                // Legacy: {lang: {name, narration, description, ...}}
```

### Resolution Priority in Client Code (translations.ts)
```typescript
function getTranslatedContent(landmark, language, field):
  1st: narrationI18n[lang]          ← HIGHEST PRIORITY
  2nd: translations[lang].narration  ← legacy
  3rd: narration field               ← base fallback
```

### The Problem
For 13 Korean landmarks (kr-*), we just updated the `narration` field (3rd priority) with rich 600+ char Korean narrations. BUT:

- `narrationI18n` has OLD short English slogans (19-37 chars) like "K-Beauty sanctuary." 
- `translations.en.narration` also has the same short text
- `narrationI18n.ko` does NOT exist for these landmarks
- Result: TTS always reads the short English slogan instead of the new rich Korean narration

### Broader Issue
This isn't just a 13-landmark problem. We need to understand:
1. How many of the 249 landmarks have mismatched narrationI18n vs narration field?
2. What's the right migration strategy for all landmarks?
3. Should we sync narrationI18n.ko FROM narration field? Or restructure the priority?

### Data Flow
- `narration` field: Korean narration (primary content language is Korean for this app)
- `narrationI18n`: Should contain per-language TTS-optimized text
- `translations`: Legacy unified translations (name + narration + description per language)
- TTS plays: `getTranslatedContent(landmark, selectedLanguage, 'narration')` → feeds to Web Speech API or OpenAI TTS

### Questions to Answer
1. Should we migrate narration → narrationI18n.ko for ALL landmarks, or only where narrationI18n.ko is missing?
2. What about narrationI18n.en - should it be updated too? Some have short slogans vs the translations.en.narration which may have proper English text.
3. Should we also sync translations[lang].narration with narrationI18n[lang]?
4. What's the safest migration approach (reversible, no data loss)?
5. How to handle the 304 landmarks that are in local data files but not yet in DB?

Please analyze and propose a comprehensive migration strategy with concrete steps.
Globpattern: "**/*translat*"
Found 101 files
Globpattern: "server/**/schema*"
No files found
Read check-translations.ts
Globpattern: "client/src/**/*narrat*"
No files found
Globpattern: "client/src/data/**/*.ts"
Found 2 files
Read useLiveTranslation.ts
Read hardcodedLandmarks.ts
Grep "getTranslatedContent.*narration" (in e:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1/client/src)
60 lines of output
Read migrate_hardcoded_data.ts
Globpattern: "server/data/landmarks*"
Found 7 files
Grep "kr-|busan|jeju|seoul|incheon" (in e:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1/server/data)
21 lines of output
Grep "id:\s*['"]kr-" (in e:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1/server/data)
2 lines of output
Grep "kr-" (in e:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1/scripts)
2 lines of output
Grep "narration_i18n|narrationI18n" (in e:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1/server)
31 lines of output
Grep "narrationI18n|narration_i18n" (in e:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1/server/routes.ts)
23 lines of output
Grep "put.*landmarks|patch.*landmarks|update.*landmarks" (in e:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1/server/routes.ts)
81 lines of output
Grep ""id":\s*"" (in e:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1/server/data/landmarks_caribbean.ts)
2 lines of output
Grep "id:\s*"" (in e:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1/server/data/landmarks_aegean.ts)
2 lines of output
Read landmarks_expansion.ts
Narration I18n Migration 전략 보고
문제 원인 (Root Cause)

사용자 언어: ko
  1순위: narrationI18n['ko']     → ❌ 없음
  2순위: translations['ko']      → ❌ 없음
  → fallback to 'en':
  1순위: narrationI18n['en']     → "K-Beauty sanctuary." (19자) ← 이걸 TTS가 읽음!
  3순위: narration 필드           → 새 한국어 나레이션 (600+자) ← 여기까지 안 옴!
narrationI18n.en에 짧은 슬로건이 들어있어 새 나레이션을 가로막고 있음.

4-Phase 전략
Phase	작업	위험도
1. Migration Script	DB 249개 전수조사 → narrationI18n.ko 채우기	LOW
2. Client 안전장치	getTranslatedContent에 quality gate 추가	LOW
3. Admin 동기화	PUT endpoint에서 narration 수정 시 narrationI18n 자동 sync	LOW
4. 로컬 데이터 통합	나머지 304개 랜드마크 DB 반영	MEDIUM
Phase 1 상세 (핵심)
전체 249개를 4가지 카테고리로 분류:

카테고리	조건	액션
A	narration에 한국어 있는데 narrationI18n.ko 없음	narration → narrationI18n.ko 복사
B	narrationI18n.en < 100자 & translations.en 더 김	translations.en.narration → narrationI18n.en
C	narrationI18n 전체 비어있고 translations에 데이터 있음	translations에서 재구축
D	이미 정상	스킵
Phase 2 상세 (안전장치)
translations.ts 수정 - narration 필드가 한국어인데 narrationI18n.ko가 없으면 base narration을 직접 반환:


if (field === 'narration' && !narrationI18n[lang]) {
  if (한글감지(landmark.narration) && lang === 'ko') {
    return landmark.narration;  // 바로 반환, fallback to en 방지
  }
}
어떻게 진행할까요?
Phase 1만 먼저 - audit + migration 스크립트 만들어서 DB 수정
Phase 1+2 동시 - DB 수정 + client 안전장치까지
전체 4 Phase - 한번에 다 진행
추천은 Phase 1+2 동시 진행 입니다. DB 데이터 수정이 근본 해결이고, client 안전장치가 재발 방지입니다.

e:\GPS-Cruise-Tour-AudioGuideNo-Wifi-1\docs\README_20210527214408.md
다국어 지원에 대해서는 어떻게 생각해 그리고 인터넷이 않되는 상황을 반영해 핸드폰에서 구동이 되도록 해야하는데 그것에 대한 대안은?
좋은 질문입니다. 현재 앱 아키텍처를 기반으로 분석해보겠습니다.

Grep "offline|indexedDB|serviceWorker|cache|no.?wifi" (in client/src)
16 lines of output
Grep "translate|getTranslatedText|targetLanguage" (in client/src/lib/audioService.ts)
12 lines of output
Read e:\GPS-Cruise-Tour-AudioGuideNo-Wifi-1\client\src\lib\offlineStorage.ts
Grep "service.worker|serviceWorker|sw\\.js|workbox" (in client)
10 lines of output
현재 상태를 종합하겠습니다.

현실적 분석 & 전략
현재 오프라인 인프라 (이미 있는 것)

✅ IndexedDB (offlineStorage.ts)
   ├── landmarks 데이터 캐시 (narration 텍스트 포함)
   ├── audio Blob 캐시 (OpenAI TTS MP3)
   └── image 캐시

✅ Service Worker + PWA (vite-plugin-pwa)
   └── 앱 셸 + API 응답 캐싱

✅ Web Speech API (브라우저 내장 TTS)
   └── 인터넷 없이도 동작 (단, 음성 품질 낮음)

✅ localStorage
   └── 번역 텍스트 캐시 (trans_ko_xxx 키)
핵심 문제: 오프라인에서 뭐가 안 되는가

          인터넷 필요              오프라인 가능
          ─────────              ──────────
TTS:      OpenAI TTS API ❌      Web Speech API ✅ (품질↓)
번역:     /api/translate ❌       narrationI18n에 미리 넣어두면 ✅
데이터:   서버 API ❌             IndexedDB 캐시 ✅
이미지:   Unsplash URL ❌        IndexedDB 캐시 ✅
다국어 + 오프라인 전략

┌─────────────────────────────────────────────────────────┐
│              현재 (인터넷 의존)                           │
│                                                         │
│  사용자 언어: es (스페인어)                                │
│  1. narrationI18n['es'] → 없음                          │
│  2. translations['es'] → 없음                           │
│  3. narration (한국어) 가져옴                             │
│  4. /api/translate 호출 → 스페인어로 번역 ← 인터넷 필요!   │
│  5. TTS 재생                                            │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│              제안 (오프라인 대응)                          │
│                                                         │
│  빌드/배포 시: 서버에서 주요 언어별 나레이션 사전 생성       │
│  narrationI18n = {                                      │
│    "ko": "경복궁은 조선의 심장...",     ← 원본 (600+자)   │
│    "en": "Gyeongbokgung Palace was...", ← 사전 번역      │
│    "ja": "景福宮は朝鮮の心臓...",       ← 사전 번역       │
│    "es": "El Palacio Gyeongbokgung...", ← 사전 번역      │
│    "zh": "景福宫是朝鲜的心脏...",       ← 사전 번역       │
│  }                                                      │
│                                                         │
│  → 오프라인에서도 바로 텍스트 사용 가능                     │
│  → Web Speech API로 해당 언어 TTS 재생                   │
│  → 번역 API 호출 불필요!                                  │
└─────────────────────────────────────────────────────────┘
구체적 실행 계획
단계	작업	인터넷	시점
A. DB 마이그레이션	narration(한국어) → narrationI18n.ko 복사 (249개)	필요	1회성
B. 다국어 사전 번역	narrationI18n.ko → en/ja/es/zh 등 번역해서 narrationI18n에 저장	필요	1회성
C. Client 안전장치	getTranslatedContent에 한글 감지 fallback 추가	불필요	코드 수정
D. 오프라인 패키지	도시별 다운로드 시 narrationI18n 전체 포함하여 IndexedDB 저장	WiFi 시	사용자 선택
E. TTS 프리캐시	주요 랜드마크 OpenAI MP3를 WiFi에서 미리 다운로드 → IndexedDB	WiFi 시	사용자 선택
오프라인 TTS 품질 비교
방식	품질	오프라인	용량	추천
Web Speech API	중하 (로봇틱)	✅ 완전	0MB	기본 폴백
OpenAI TTS 프리캐시	상 (자연스러움)	✅ 캐시 시	~50KB/문장	WiFi에서 사전 다운
OpenAI TTS 실시간	상	❌	0MB	인터넷 있을 때만
추천 우선순위
지금 바로 해야 할 것 (Phase 1+2):

narration → narrationI18n.ko 복사 (13개 → 전체 249개)
getTranslatedContent 안전장치 추가
짧은 슬로건 narrationI18n.en 교체
다음 단계:
4. 서버 스크립트로 narrationI18n에 주요 5개 언어 사전 번역 (ko/en/ja/zh/es)
5. 오프라인 다운로드 시 narrationI18n 전체 포함

결론: 실시간 번역 의존을 없애고, narrationI18n에 미리 다국어 나레이션을 채워두는 것이 No-WiFi 앱의 핵심 전략입니다. Phase 1+2부터 진행할까요?

그럼 지금 해야하는 작업들을 동시에 병렬로 작업하자 todos를 반영해서 스킬 BMAD의 기획과  LANGGRAPH의 컨트롤하에 진행해 보자
Update Todos

Phase 1A: DB 전수조사 - 249개 랜드마크 narrationI18n 상태 audit

Phase 1B: narration(한국어) → narrationI18n.ko 마이그레이션 (카테고리A)

Phase 1C: 짧은 narrationI18n.en 슬로건 → translations.en.narration 교체 (카테고리B)

Phase 1D: narrationI18n 비어있는 랜드마크 translations에서 재구축 (카테고리C)

Phase 2: Client getTranslatedContent 안전장치 추가 (translations.ts)

Phase 3: 검증 - TTS 매칭 최종 확인 (13개 + 전체)
Phase 1A audit와 Phase 2 client 수정을 병렬로 시작합니다.

Agent:Phase 1A: DB audit all 249
IN
Connect to NeonDB and run a comprehensive audit of ALL 249 landmarks' narrationI18n status. 

DB connection: postgresql://neondb_owner:npg_RxOvMV2BQ4Lo@ep-summer-smoke-a1ly2y42-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

Create and run a script `tmp/audit_narration_i18n.ts` that:

1. Queries ALL landmarks: id, city_id, name, narration, narration_i18n, translations, LENGTH(narration)
2. Classifies each into categories:
   - **Category A**: `narration` field has Korean text (contains hangul ㄱ-힣), but `narrationI18n.ko` is missing/empty → needs copy narration → narrationI18n.ko
   - **Category B**: `narrationI18n.en` exists but < 100 chars, AND `translations.en.narration` is longer → needs replace
   - **Category C**: `narrationI18n` is entirely null/empty, but `translations` has narration entries → needs rebuild from translations
   - **Category D**: Already good (narrationI18n has adequate content)
3. Output a JSON summary:
   - Total count per category
   - List of IDs per category
   - For Category A: show narration length and whether narrationI18n.ko exists
   - For Category B: show narrationI18n.en length vs translations.en.narration length

Use `import { neon } from '@neondatabase/serverless'` and run with `npx tsx`.

Print results clearly. This is RESEARCH ONLY - do NOT modify any data.
Agent:Phase 2: Fix client translations.ts
IN
Read and modify the file `client/src/lib/translations.ts` to add a Korean narration fallback safety mechanism.

Current code at line 3-42 has `getTranslatedContent` function with priority:
1. narrationI18n[lang]
2. translations[lang].narration  
3. English fallback from narrationI18n['en'] or translations['en']
4. base narration field

THE BUG: When lang='ko' and narrationI18n.ko is missing, it falls back to narrationI18n.en which has a short English slogan (19-37 chars), never reaching the base `narration` field which has rich Korean content (600+ chars).

FIX NEEDED: Add a special case BEFORE the English fallback (between current step 2 and 3). When:
- field is 'narration'
- requested language is 'ko' (or narrationI18n.ko is missing)
- the base `landmark.narration` field contains Korean characters (hangul regex: /[가-힣]/)
- AND the base narration is longer than what would be returned from English fallback

Then return `landmark.narration` directly instead of falling through to the English slogan.

Also add a general quality gate: if narrationI18n[lang] exists but is < 50 chars, and the base narration field is > 200 chars, prefer the base narration.

Read the file first, then make minimal targeted edits. Keep the existing code structure.
Grep "@neondatabase/serverless" (in e:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1, glob: package.json)
8 lines of output
Read translations.ts
Edit translations.ts
Added 6 lines
Edit translations.ts
Added 10 lines
Read translations.ts
Write audit_narration_i18n.ts
221 lines
import { neon } from '@neondatabase/serverless';

const sql = neon(
  'postgresql://neondb_owner:npg_RxOvMV2BQ4Lo@ep-summer-smoke-a1ly2y42-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require'
);

interface Landmark {
  id: number;
  city_id: number;
  name: string;
  narration: string | null;
  narration_i18n: Record<string, string> | null;
  translations: Record<string, { narration?: string; name?: string }> | null;
  narration_length: number | null;
}

function containsHangul(text: string): boolean {
  return /[\uAC00-\uD7A3\u3131-\u3163]/.test(text);
}

async function main() {
  console.log('Fetching all landmarks...\n');

  const rows = (await sql`
    SELECT
      id,
      city_id,
      name,
      narration,
      "narrationI18n" as narration_i18n,
      translations,
      LENGTH(narration) as narration_length
    FROM landmarks
    ORDER BY id
  `) as Landmark[];

  console.log(`Total landmarks fetched: ${rows.length}\n`);

  const categoryA: {
    id: number;
    name: string;
    narration_length: number | null;
    narrationI18nKoExists: boolean;
    narrationI18nKoLength: number;
  }[] = [];
  const categoryB: {
    id: number;
    name: string;
    narrationI18nEnLength: number;
    translationsEnNarrationLength: number;
  }[] = [];
  const categoryC: {
    id: number;
    name: string;
    translationLangs: string[];
  }[] = [];
  const categoryD: {
    id: number;
    name: string;
  }[] = [];

  for (const row of rows) {
    const narration = row.narration || '';
    const ni18n = row.narration_i18n || {};
    const trans = row.translations || {};

    const hasHangulNarration = narration.length > 0 && containsHangul(narration);
    const ni18nKo = ni18n?.ko || '';
    const ni18nEn = ni18n?.en || '';
    const transEnNarration = trans?.en?.narration || '';

    // Check if narrationI18n is entirely empty/null
    const ni18nIsEmpty =
      !row.narration_i18n ||
      Object.keys(row.narration_i18n).length === 0 ||
      Object.values(row.narration_i18n).every((v) => !v || v.trim() === '');

    // Check if translations has any narration entries
    const transNarrationLangs = Object.entries(trans)
      .filter(([, v]) => v?.narration && v.narration.trim().length > 0)
      .map(([k]) => k);

    let classified = false;

    // Category A: narration has Korean but narrationI18n.ko missing/empty
    if (hasHangulNarration && ni18nKo.trim().length === 0) {
      categoryA.push({
        id: row.id,
        name: row.name,
        narration_length: row.narration_length,
        narrationI18nKoExists: !!ni18n?.ko,
        narrationI18nKoLength: ni18nKo.length,
      });
      classified = true;
    }

    // Category B: narrationI18n.en exists but < 100 chars, AND translations.en.narration is longer
    if (
      ni18nEn.trim().length > 0 &&
      ni18nEn.trim().length < 100 &&
      transEnNarration.trim().length > ni18nEn.trim().length
    ) {
      categoryB.push({
        id: row.id,
        name: row.name,
        narrationI18nEnLength: ni18nEn.trim().length,
        translationsEnNarrationLength: transEnNarration.trim().length,
      });
      classified = true;
    }

    // Category C: narrationI18n entirely null/empty, but translations has narration entries
    if (ni18nIsEmpty && transNarrationLangs.length > 0) {
      categoryC.push({
        id: row.id,
        name: row.name,
        translationLangs: transNarrationLangs,
      });
      classified = true;
    }

    // Category D: already good (not in any problem category)
    if (!classified) {
      categoryD.push({
        id: row.id,
        name: row.name,
      });
    }
  }

  const summary = {
    total: rows.length,
    counts: {
      categoryA_korean_narration_missing_i18n_ko: categoryA.length,
      categoryB_short_en_i18n_longer_translation: categoryB.length,
      categoryC_empty_i18n_has_translations: categoryC.length,
      categoryD_already_good: categoryD.length,
    },
    categoryA: {
      description:
        'narration field has Korean text, but narrationI18n.ko is missing/empty. Needs: copy narration → narrationI18n.ko',
      count: categoryA.length,
      ids: categoryA.map((r) => r.id),
      details: categoryA,
    },
    categoryB: {
      description:
        'narrationI18n.en exists but < 100 chars, AND translations.en.narration is longer. Needs: replace with translations version',
      count: categoryB.length,
      ids: categoryB.map((r) => r.id),
      details: categoryB,
    },
    categoryC: {
      description:
        'narrationI18n is entirely null/empty, but translations has narration entries. Needs: rebuild from translations',
      count: categoryC.length,
      ids: categoryC.map((r) => r.id),
      details: categoryC,
    },
    categoryD: {
      description: 'Already good - narrationI18n has adequate content',
      count: categoryD.length,
      ids: categoryD.map((r) => r.id),
    },
  };

  console.log(JSON.stringify(summary, null, 2));

  // Print a readable summary table
  console.log('\n========================================');
  console.log('  NARRATION I18N AUDIT SUMMARY');
  console.log('========================================');
  console.log(`Total landmarks: ${rows.length}`);
  console.log(`Category A (Korean narration, missing i18n.ko): ${categoryA.length}`);
  console.log(`Category B (short i18n.en, longer translation): ${categoryB.length}`);
  console.log(`Category C (empty i18n, has translations):      ${categoryC.length}`);
  console.log(`Category D (already good):                      ${categoryD.length}`);
  console.log(
    `Sum of A+B+C+D: ${categoryA.length + categoryB.length + categoryC.length + categoryD.length}`
  );
  console.log(
    `(Note: landmarks can appear in multiple problem categories A/B/C, but only in D if in none)`
  );
  console.log('========================================\n');

  if (categoryA.length > 0) {
    console.log('--- Category A Details ---');
    for (const r of categoryA) {
      console.log(
        `  ID ${r.id}: "${r.name}" | narration_len=${r.narration_length} | i18n.ko exists=${r.narrationI18nKoExists} len=${r.narrationI18nKoLength}`
      );
    }
    console.log('');
  }

  if (categoryB.length > 0) {
    console.log('--- Category B Details ---');
    for (const r of categoryB) {
      console.log(
        `  ID ${r.id}: "${r.name}" | i18n.en len=${r.narrationI18nEnLength} vs translations.en.narration len=${r.translationsEnNarrationLength}`
      );
    }
    console.log('');
  }

  if (categoryC.length > 0) {
    console.log('--- Category C Details ---');
    for (const r of categoryC) {
      console.log(
        `  ID ${r.id}: "${r.name}" | translation langs with narration: [${r.translationLangs.join(', ')}]`
      );
    }
    console.log('');
  }
}

main().catch((err) => {
  console.error('Error:', err);
  process.exit(1);
});
Bash Run the narration i18n audit script
IN
cd e:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1 && npx tsx tmp/audit_narration_i18n.ts 2>&1

OUT
Exit code 1
Fetching all landmarks...

Error: NeonDbError: column "narrationI18n" does not exist
    at execute (file:///E:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1/node_modules/@neondatabase/serverless/index.mjs:1556:55)
    at process.processTicksAndRejections (node:internal/process/task_queues:104:5)
    at async main (E:\GPS-Cruise-Tour-AudioGuideNo-Wifi-1\tmp\audit_narration_i18n.ts:24:17) {
  severity: 'ERROR',
  code: '42703',
  detail: undefined,
  hint: 'Perhaps you meant to reference the column "landmarks.narration_i18n".',
  position: '73',
  internalPosition: undefined,
  internalQuery: undefined,
  where: undefined,
  schema: undefined,
  table: undefined,
  column: undefined,
  dataType: undefined,
  constraint: undefined,
  file: 'parse_relation.c',
  line: '3723',
  routine: 'errorMissingColumn',
  sourceError: undefined
}
Edit audit_narration_i18n.ts
Modified
Bash Run the audit script again
IN
cd e:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1 && npx tsx tmp/audit_narration_i18n.ts 2>&1

OUT
<persisted-output>
Output too large (88.7KB). Full output saved to: C:\Users\jitne\.claude\projects\e--GPS-Cruise-Tour-AudioGuideNo-Wifi-1\862ff5f4-e30d-4fbe-8f2c-a7e8ae6fc012\tool-results\b0mp4g8ss.txt

Preview (first 2KB):
Fetching all landmarks...

Total landmarks fetched: 249

{
  "total": 249,
  "counts": {
    "categoryA_korean_narration_missing_i18n_ko": 249,
    "categoryB_short_en_i18n_longer_translation": 0,
    "categoryC_empty_i18n_has_translations": 0,
    "categoryD_already_good": 0
  },
  "categoryA": {
    "description": "narration field has Korean text, but narrationI18n.ko is missing/empty. Needs: copy narration → narrationI18n.ko",
    "count": 249,
    "ids": [
      "afternoon-tea-london",
      "alaska_wildlife_center",
      "anchorage_museum",
      "anne_frank_house",
      "arc_triomphe",
      "atomium",
      "barcelona_구엘_공원_park_gell",
      "barcelona_디스프루타르_disfrutar",
      "barcelona_사그라다_파밀리아_sagrada_familia",
      "barcelona_프라이빗_선셋_요트_크루즈_private_sunset_",
      "basilica-santo-nino",
      "batu-caves",
      "batu-caves-tour",
      "batu-caves-tour-kl",
      "big-buddha-phuket",
      "big_ben",
      "borghese-gift-shop-rome",
      "british_museum",
      "buckingham_palace",
      "bus_gamcheon",
      "busan_f1963_복합문화공간_f1963",
      "busan_더베이_101_the_bay_101",
      "busan_더베이_101_요트클럽_다이닝_the_bay_101_y",
      "busan_해동_용궁사_haedong_yonggungsa_temp",
      "busan_해동용궁사_haedong_yonggungsa_templ",
      "casa_batllo",
      "castel_santangelo",
      "cebu-heritage-monument",
      "cebu-island-hopping",
      "central-market-kl",
      "chain_bridge",
      "charles_bridge",
      "chinatown-heritage-centre",
      "chinatown-night-market-kl",
      "colosseum",
      "colosseum-memories-rome",
      "colosseum-underground-tour",
      "denali_national_park",
      "eiffel_tower",
      "fort-cornwallis",
      "fort-san-pedro",
      "gaisano-grand-mall-talamban",
      "gamla_stan",
      "gardens-by-the-bay",
      "gardens-by-the-bay-night",
      "gardens-light-show-singapore",
      "george-town-unesco-site",
      "georgetown-street-art",
      "glacier_bay",
      "grand_place",
      "harry-potter-studio-tour",
...
</persisted-output>
Read C:\Users\jitne\.claude\projects\e--GPS-Cruise-Tour-AudioGuideNo-Wifi-1\862ff5f4-e30d-4fbe-8f2c-a7e8ae6fc012\tool-results\b0mp4g8ss.txt (lines 2-51)
Read C:\Users\jitne\.claude\projects\e--GPS-Cruise-Tour-AudioGuideNo-Wifi-1\862ff5f4-e30d-4fbe-8f2c-a7e8ae6fc012\tool-results\b0mp4g8ss.txt (from line 871)
Update Todos

Phase 1A: DB 전수조사 - 249개 랜드마크 narrationI18n 상태 audit

Phase 1B: narration(한국어) → narrationI18n.ko 마이그레이션 (249개 전체)

Phase 1C: 짧은 narrationI18n.en 슬로건 → translations.en.narration 교체 (카테고리B)

Phase 1D: narrationI18n 비어있는 랜드마크 translations에서 재구축 (카테고리C)

Phase 2: Client getTranslatedContent 안전장치 추가 (translations.ts)

Phase 3: 검증 - TTS 매칭 최종 확인 (13개 + 전체)
Audit 결과: 249개 전부 Category A (narrationI18n.ko 없음). Phase 1B 마이그레이션과 client 수정 확인을 병렬 진행합니다.

Agent:Phase 1B: Migrate narration to i18n.ko
IN
Create and run a migration script `tmp/migrate_narration_to_i18n_ko.ts` that copies the `narration` field into `narration_i18n` as the `ko` key for ALL 249 landmarks in the database.

DB connection: postgresql://neondb_owner:npg_RxOvMV2BQ4Lo@ep-summer-smoke-a1ly2y42-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

The script should:

1. Query all landmarks: `SELECT id, narration, narration_i18n FROM landmarks`
2. For each landmark:
   - If `narration_i18n` is null/empty: set it to `{"ko": narration_value}`
   - If `narration_i18n` exists but has no `ko` key: add `ko` key with `narration` value (preserve existing keys like `en`)
   - If `narration_i18n.ko` already exists and is longer: skip (don't overwrite better content)
3. Use batch UPDATE with `Promise.all` for speed, but chunk into groups of 25 to avoid overloading
4. Print progress: how many updated, skipped
5. After migration, verify by counting how many now have `narration_i18n` with `ko` key

Use `import { neon } from '@neondatabase/serverless'` and run with `npx tsx`.

IMPORTANT: Actually run the script and report the results. This IS an execution task, not research.
Read e:\GPS-Cruise-Tour-AudioGuideNo-Wifi-1\client\src\lib\translations.ts
Grep "@neondatabase/serverless" (in e:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1, glob: package.json)
8 lines of output
Read audit_narration_i18n.ts
Write migrate_narration_to_i18n_ko.ts
99 lines
import { neon } from '@neondatabase/serverless';

const sql = neon(
  'postgresql://neondb_owner:npg_RxOvMV2BQ4Lo@ep-summer-smoke-a1ly2y42-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require'
);

interface LandmarkRow {
  id: number;
  narration: string | null;
  narration_i18n: Record<string, string> | null;
}

async function main() {
  console.log('=== Migration: Copy narration -> narration_i18n.ko ===\n');

  // 1. Query all landmarks
  const rows = (await sql`
    SELECT id, narration, narration_i18n FROM landmarks
  `) as LandmarkRow[];

  console.log(`Total landmarks fetched: ${rows.length}\n`);

  let updated = 0;
  let skipped = 0;
  let noNarration = 0;

  // Build list of updates
  const updates: { id: number; newI18n: Record<string, string> }[] = [];

  for (const row of rows) {
    const { id, narration, narration_i18n } = row;

    // Skip if no narration to copy
    if (!narration || narration.trim() === '') {
      noNarration++;
      continue;
    }

    // Case: narration_i18n is null/empty -> set to {"ko": narration}
    if (!narration_i18n || Object.keys(narration_i18n).length === 0) {
      updates.push({ id, newI18n: { ko: narration } });
      continue;
    }

    // Case: narration_i18n exists but no ko key -> add ko
    if (!narration_i18n.ko) {
      updates.push({ id, newI18n: { ...narration_i18n, ko: narration } });
      continue;
    }

    // Case: ko exists - skip if existing ko is longer (better content)
    if (narration_i18n.ko.length >= narration.length) {
      skipped++;
      continue;
    }

    // ko exists but narration is longer -> overwrite
    updates.push({ id, newI18n: { ...narration_i18n, ko: narration } });
  }

  console.log(`Updates to perform: ${updates.length}`);
  console.log(`Skipped (ko already longer): ${skipped}`);
  console.log(`No narration to copy: ${noNarration}\n`);

  // 3. Batch update in chunks of 25
  const chunkSize = 25;
  for (let i = 0; i < updates.length; i += chunkSize) {
    const chunk = updates.slice(i, i + chunkSize);
    await Promise.all(
      chunk.map(({ id, newI18n }) =>
        sql`UPDATE landmarks SET narration_i18n = ${JSON.stringify(newI18n)}::jsonb WHERE id = ${id}`
      )
    );
    updated += chunk.length;
    console.log(`  Updated ${updated} / ${updates.length}`);
  }

  console.log(`\n=== Migration complete ===`);
  console.log(`  Updated: ${updated}`);
  console.log(`  Skipped (ko already longer): ${skipped}`);
  console.log(`  No narration: ${noNarration}`);
  console.log(`  Total: ${updated + skipped + noNarration}\n`);

  // 5. Verify
  const verification = await sql`
    SELECT COUNT(*) as total,
           COUNT(*) FILTER (WHERE narration_i18n IS NOT NULL AND narration_i18n ? 'ko') as with_ko
    FROM landmarks
  `;
  console.log('=== Verification ===');
  console.log(`  Total landmarks: ${verification[0].total}`);
  console.log(`  With narration_i18n.ko: ${verification[0].with_ko}`);
}

main().catch((err) => {
  console.error('Migration failed:', err);
  process.exit(1);
});
Bash Run the migration script
IN
cd e:/GPS-Cruise-Tour-AudioGuideNo-Wifi-1 && npx tsx tmp/migrate_narration_to_i18n_ko.ts

OUT
=== Migration: Copy narration -> narration_i18n.ko ===

Total landmarks fetched: 249
