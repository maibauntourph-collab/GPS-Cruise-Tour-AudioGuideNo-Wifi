# 🔍 데이터베이스 및 동기화 진단 보고서 (@에이? 대응)

사용자님(교수님), 긴급 보고 드립니다! 🎓
세부(Cebu) 지역 데이터가 화면에 나오지 않는 이유와 '@에이?' 요청에 대한 추천 에이전트/프롬프트를 정리했습니다.

## 1. @에이? (추천 에이전트 및 스킬)

지금처럼 데이터 정합성이나 DB 연결 문제가 의심될 때는 다음 조합을 추천합니다.

| 구분 | 추천 항목 | 이유 |
| :--- | :--- | :--- |
| **추천 에이전트** | **쿼리 마스터 (Query Master)** | 데이터 무결성, 스키마 분석, SQL 최적화의 대가입니다. |
| **사용 스킬** | `query_master` | Neon DB와 Drizzle ORM의 정합성을 칼같이 체크합니다. |
| **추천 MCP** | `sequential-thinking` | 복잡한 데이터 동기화 문제를 단계별로 추론하여 해결합니다. |

**[추천 프롬프트]**
> "현재 세부(Cebu) 지역 명소 데이터가 DB와 소스 코드 간에 동기화되지 않고 있어. Query Master의 권한으로 실시간 DB 로그를 분석하고, `landmarks` 테이블에 세부 명소가 정상적으로 삽입되어 있는지, 그리고 ID 규칙이 일치하는지 전수 조사해줘."

## 2. 긴급 진단 결과 (세부 데이터 실종 사건)

- **현상**: `Home.tsx`에서 세부 지도를 보고 계시지만, 랜드마크 핀이 하나도 보이지 않음.
- **원인 분석**:
  1. **소스 코드 부재**: `server/data/landmarks.ts`에서 `cityId: "cebu"`를 찾을 수 없습니다. (혹시 `cebucity` 등 다른 ID를 쓰는지 확인 중)
  2. **DB 시딩 누락**: 고도화 스크립트 실행 로그에도 세부 명소를 처리한 기록이 없습니다.
  3. **환경 변수 체크**: `NOWIFIGPSTOURS` DB 연결은 정상이지만, 대상 데이터 자체가 없습니다.

## 3. 즉시 조치 계획

1. `landmarks.ts` 전체를 다시 훑어 세부 데이터의 **정확한 ID와 cityId**를 찾아내겠습니다.
2. 만약 데이터 자체가 누락되었다면, 기존 백업 데이터나 AI를 통해 세부 명소 리스트를 즉시 생성하여 DB에 주입하겠습니다.
3. 작업 완료 후 다시 고도화 스크립트를 돌려 V3 프리미엄 내용을 채워 넣겠습니다.

**교수님, Query Master를 즉시 투입하여 세부 데이터를 복구해도 될까요?**
