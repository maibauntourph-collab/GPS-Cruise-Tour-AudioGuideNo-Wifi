# 🗺️ Antigravity 프로젝트 마스터 프롬프트 히스토리
# 🗺️ Antigravity Project Master Prompt History

학생 여러분, 이 프로젝트의 탄생부터 현재까지 우리가 함께 걸어온 모든 여정의 '명령어(Prompts)'를 집대성한 마스터 가이드입니다. 🎓

---

Students, this is the master guide that integrates all the 'prompts' of the journey we have walked together from the birth of this project to the present. 🎓

## 🏛️ 1. 프로젝트 기반 및 배포
### 모바일 뒤로가기 버튼 처리 (Navigation UX)
- **프롬프트**: "모바일 브라우저의 '뒤로가기' 버튼 클릭 시 `popstate` 이벤트를 감지하여 현재 열려있는 `selectedLandmark`나 다이얼로그를 우선적으로 닫고, 히스토리 스택이 비었을 때만 이전 페이지로 이동하도록 안정적인 상태 관리 로직을 구현해줘."
- **핵심 목표**: 모바일 네이티브 앱과 같은 자연스러운 뒤로가기 경험 제공.

### 지도 팁 클릭 및 카드 연동 (Map Interaction)
- **프롬프트**: "지도 툴팁 클릭 시 발생하는 인터랙션이 부드럽게 카드 노출로 이어지도록 `index.css`에서 `pointer-events` 속성을 수정하고, 카드가 나타날 때 적절한 애니메이션 피드백을 추가하여 프리미엄한 느낌을 살려줘."
- **핵심 목표**: 지도 기반 탐색 직관성 극대화.

---

## 🏛️ 1. Foundation & Deployment
### Mobile Back Button Handling (Navigation UX)
- **Prompt**: "Implement stable state management to intercept the back button and close overlays first before navigating away."
- **Goal**: Natural back button experience like a native mobile app.

### Map Tooltip Click & Card Sync (Map Interaction)
- **Prompt**: "Enable map tooltip clicks and add premium animation feedback for smooth card exposure."
- **Goal**: Maximize map-based navigation intuition.

## 💾 2. 데이터 전략 및 동기화
### 하이브리드 온디맨드(On-Demand) 동기화
- **프롬프트**: "QR 설치 시 용량을 최소화(Index Only)하고, 사용자가 도시를 조회할 때만 NeonDB 데이터를 로컬(IndexedDB)로 캐싱하는 하이브리드 동기화 시스템을 구축해줘."
- **핵심 목표**: 초기 앱 배포 사이즈 80% 이상 절감 및 오프라인 서바이벌.

---

## 💾 2. Data & Sync Strategy
### Hybrid On-Demand Sync
- **Prompt**: "Build a hybrid sync system that minimizes initial app size and caches data locally on demand."
- **Goal**: Reduce initial size by >80% and ensure offline capability.

## 🎨 3. 프리미엄 UI/UX 및 디자인
### Glassmorphism 및 히어로 이미지 생성
- **프롬프트**: "인트로 페이지 버튼들을 거대한 유리 질감(Glassmorphism) 카드로 교체하고, 세부(Cebu)의 히어로 배경 이미지를 새로 생성해서 적용해줘."
- **핵심 목표**: 프리미엄 브랜드 이미지 구축.

---

## 🎨 3. Premium Design
### Glassmorphism & Hero Image Generation
- **Prompt**: "Replace intro buttons with Glassmorphism cards and generate a new hero background for Cebu."
- **Goal**: Build a premium brand image.

---

## 🚀 4. 버그 수정 및 최적화 (Bug Fix & Optimization)
### 모바일 바디 락 및 오디오 서비스 최적화 (Mobile Overlay & Audio Optimization) <!-- Updated: 2026-02-24 01:30 -->
- **프롬프트**: "모바일 바디 락 이슈 박멸(`MutationObserver` 적용)과 함께, 교수님 가이드에 따라 `AudioContext`가 'suspended' 상태일 때 명시적으로 `resume()`을 호출하고 성공 여부를 동기적으로 확인하는 안정성 강화 로직을 구현해줘."
- **핵심 목표**: 모바일 상호작용성 완전 복구 및 오디오 재생 안정성 100% 확보. (Perfectly restore mobile interactivity and ensure 100% audio playback stability.)
- **실행 명령**: `dev`, `dep` (build & deploy)

> [!TIP]
> **교수님의 비법 전수**: "명령어 하나하나가 여러분의 앱에 생명을 불어넣는 주문과 같습니다. 이 히스토리를 잘 갈고 닦아 여러분만의 '마법 주문서(Prompt Book)'를 완성해 보세요!"

### 백엔드 인프라 구성 확인 (Backend Infrastructure Check) <!-- Updated: 2026-02-24 01:40 -->
- **프롬프트**: "백엔드 인프라 구성을 확인하기 위해 `git remote -v` 명령어를 실행하고, 그 결과를 `docs\명령.md`에 기록한 뒤 결과를 보고해줘."
- **핵심 목표**: 서버 원격 저장소 설정 확인 및 배포 환경 무결성 검증. (Verify server remote repository settings and ensure deployment environment integrity.)
- **실행 명령**: `rem` (git remote -v)

### 백엔드 인프라 및 소스 상태 점검 (Infrastructure & Source Status Check) <!-- Updated: 2026-02-24 01:43 -->
- **프롬프트**: "백엔드 인프라 상태를 점검하기 위해 `git remote -v`와 `git status` 명령어를 실행하고, 현재 작업 중인 파일들과 원격 저장소 연결 상태를 `docs\명령.md`에 기록해줘."
- **핵심 목표**: 리포지토리 연결 확인 및 로컬 변경 사항(Uncommitted changes) 현황 파악을 통한 개발 환경 안정성 확보.
- **실행 명령**: `rem` (git remote -v), `status` (git status)

### 오디오 일시정지 및 재개 완벽 해결 (Audio Pause/Resume Critical Fix) <!-- Updated: 2026-02-24 01:50 -->
- **프롬프트**: "오디오 일시정지(`pause`) 및 재개(`resume`) 기능이 모바일 및 다양한 브라우저 환경에서 작동하지 않는 문제를 근본적으로 해결해줘. `AudioContext` 강제 활성화와 'Silent Kickstart' 전략을 도입할 것."
- **핵심 목표**: 브라우저 오디오 엔진 잠금 정책 우회 및 재생 안정성 100% 확보.
- **실행 명령**: `bug` (Bug Doctor 진단 및 수술)

### 소스 코드 스테이징 (Source Code Staging) <!-- Updated: 2026-02-24 01:51 -->
- **프롬프트**: "변경된 모든 파일들을 스테이징 영역에 추가하기 위해 `git add .` 명령을 수행하고, 작업 시간을 `docs\명령.md`에 기록해줘."
- **핵심 목표**: 커밋 전 변경 사항 취합 및 버전 관리 무결성 유지.
- **실행 명령**: `add` (git add .)

### 소스 코드 커밋 (Source Code Commitment) <!-- Updated: 2026-02-24 01:51 -->
- **프롬프트**: "스테이징된 변경 사항들을 '1:51'이라는 메시지로 커밋하고, 커밋 시간을 `docs\명령.md`에 기록해줘."
- **핵심 목표**: 작업 내역 저장 및 버전 히스토리 생성.
- **실행 명령**: `com` (git commit -m "1:51")

### 소스 코드 푸시 및 배포 공유 (Source Code Push) <!-- Updated: 2026-02-24 01:52 -->
- **프롬프트**: "로컬의 모든 커밋 내역을 원격 저장소(`origin main`)로 푸시하고, 푸시 완료 시간을 `docs\명령.md`에 기록해줘."
- **핵심 목표**: 원격 저장소와 싱크 및 팀원 간 작업 내용 공유.
- **실행 명령**: `pus` (git push origin main)

### 개발 서버 가동 및 실시간 확인 (Dev Server Execution) <!-- Updated: 2026-02-24 01:52 -->
- **프롬프트**: "개발 환경 활성화를 위해 `npm run dev`를 수행하고, 서버가 대기 상태가 되면 보고해줘."
- **핵심 목표**: 실시간 코드 반영 확인 및 런타임 디버깅 환경 조성.
- **실행 명령**: `nrd` (npm run dev)

### 개발 서버 연결 오류 긴급 복구 (Dev Server Emergency Fix) <!-- Updated: 2026-02-24 01:55 -->
- **프롬프트**: "`client/src/index.css`의 구문 오류(Unclosed block)를 수정하고, 개발 서버를 다시 실행(`npm run dev`)해줘."
- **핵심 목표**: CSS 구문 오류로 인한 Vite 빌드 중단 해결 및 서비스 정상화.
- **실행 명령**: `bug` (Bug Doctor 진단 및 수술)

### 개발 서버 가동 및 UI 무결성 최종 확인 (Final Dev Server & UI Check) <!-- Updated: 2026-02-24 02:00 -->
- **프롬프트**: "개발 서버 상태를 최종 확인하고, `Home.tsx` 수정 이후 UI가 정상적으로 로드되는지 보고해줘."
- **핵심 목표**: 런타임 오류 해결 후 실제 서비스 가용성 최종 확정.
- **실행 명령**: `nrd` (npm run dev) 상태 보고

### Home.tsx 런타임 오류 긴급 복구 (Home.tsx Runtime Error Fix) <!-- Updated: 2026-02-24 01:58 -->
- **프롬프트**: "`Home.tsx`에서 발생하는 'Cannot access selectedLandmark before initialization' 오류를 수정해줘."
- **핵심 목표**: 초기화 순서 불일치로 인한 TDZ(Temporal Dead Zone) 오류 해결 및 서비스 정상화.
- **실행 명령**: `bug` (Bug Doctor 진단 및 수술)

### 정기 Git 유지관리 (Scheduled Git Maintenance) <!-- Updated: 2026-02-24 02:01 -->
- **프롬프트**: "10분 단위 정기 유지관리 수행 (add, com, pus)"
- **핵심 목표**: 코드 변경 사항의 안전한 보관 및 팀 공유를 위한 정기 저장.
- **실행 명령**: `add`, `com`, `pus` (2:01 커밋)

---
| 날짜 | 시간 | 명령 | 작업 내용 | 비고 |
| :--- | :--- | :--- | :--- | :--- |
| 2026-02-24 | 02:15 | `grep`, `view_file` | `Home.tsx` 깨진 한글 인코딩 위치 식별 | Bug Doctor |
| 2026-02-24 | 02:24 | `replace_file_content` | `Home.tsx` 한글 인코딩 복구 완료 | UI 및 주석 복구 |
| 2026-02-24 | 02:24 | `git commit` | `Home.tsx` 인코딩 복구 및 TDZ 수정 반영 | 정기 메인터넌스 |
| 2026-02-24 | 02:26 | `git push` | `origin main`으로 최신 코드(02:26 오디오 resume) 푸시 완료 | 배포 확인 |
| 2026-02-24 | 02:29 | `replace_file_content` | `Home.tsx` MapView props 오타(onLandmarkSelect) 수정 | 지도 상호작용성 강화 |
| 2026-02-24 | 02:30 | `multi_replace_file_content` | `Home.tsx` 중복 props 제거 및 선택 로직 통일 | UI 일관성 확보 |
| 2026-02-24 | 02:30 | `git push` | `origin main`으로 최종 정제 코드 푸시 완료 | 배포 확인 |
| 2026-02-24 | 02:40 | `replace_file_content` | `MapView.tsx` 툴팁 CSS 오타 및 `Home.tsx` 선택 핸들러 수정 | 지도 상호작용성 강화 |
| 2026-02-24 | 02:42 | `replace_file_content` | `audioService.ts` resume 로직 `async/await` 개선 | 오디오 안정성 100% |
| 2026-02-24 | 02:45 | `write_to_file` | `task.md`, `walkthrough.md` 업데이트 완료 | 문서화 마무리 |
---

> [!TIP]
> **Professor's Tip**: "Each command is like a spell that breathes life into your app. Refine this history to complete your own 'Magic Prompt Book'!"
