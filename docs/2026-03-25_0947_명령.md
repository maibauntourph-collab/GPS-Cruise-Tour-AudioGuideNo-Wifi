# 📋 작업 명령서 - 2026-03-25 09:47

## 1. 개요
'플로팅 아이콘(되돌아가기)' 및 제 65-66장의 변경 사항이 모두 통합된 코드베이스를 Cloudflare Workers 환경으로 최종 프로덕션 배포(`npm run deploy`)합니다.

## 2. 추천 에이전트 및 프롬프트 (@에이)

### 🤖 추천 에이전트
1. **Server Park (AI 백엔드 팀장)**:
   * **역할**: 프로덕션 빌드 시스템(Vite, esbuild) 관리, `wrangler deploy` 최적화 및 에지(Edge) 인프라 배포 관장.
2. **Bug Doctor (AI 프로그래밍 전문가)**:
   * **역할**: 빌드 중 발생할 수 있는 TypeScript 모듈 오류 및 Type 에러를 실시간 모니터링하고 방어.

### 📝 추천 프롬프트
```markdown
안녕 백엔드 팀장 서버 팍, 버그 닥터!
우리가 열심히 만든 PWA 오버레이와 되돌아가기 기능이 담긴 최신 버전을 
이제 Cloudflare Workers 엣지 서버에 공식 배포하고 싶어.

1. **빌드 관리 (Server Park)**:
   - `npm run deploy` 명령어를 실행하여 프론트엔드 최적화 빌드와 백엔드 번들링을 수행해줘.
   - 워커 인프라 설정이 올바르게 배포되었는지 `wrangler` 출력 로그를 모니터링해.
2. **배포 검증 (Bug Doctor)**:
   - TypeScript 빌드 중 혹은 Rollup 컴파일 중 에러가 나는지 실시간으로 터미널을 지켜보고,
   - 만일 문제가 발생하면 학생들의 이해를 돕기 위해 친절하게 에러 원인을 설명해주렴.

실시간 진행 사항을 터미널과 에디터에 병렬로 에코해주고, 
히스토리 북 제 67장에 "최종 무중단 배포(Production Deployment)" 작업을 기록해줘!
```

## 3. 진행 상황 체크리스트
- [x] 터미널 명령어 `npm run deploy` 실행 개시
- [x] 추천 에이전트 및 프롬프트 제안
- [x] 히스토리 북 업데이트 (제 67장)
- [ ] 정적 자산(Asset) 빌드 완수 대기
- [ ] 서버 번들 성공 및 Cloudflare Workers 퍼블리싱 (진행중)
