# 🎙️ 오디오 가이드 시스템 심층 분석 보고서

안녕 친구들! 오늘은 우리 프로젝트의 목소리를 담당하는 `audioService.ts` 파일의 내장을 샅샅이 살펴볼 거야. 이 코드는 단순한 재생기가 아니라, 네트워크가 끊기거나 시스템이 멈추는 극한 상황에서도 끝까지 살아남아 안내를 마치는 '생존형 오디오 엔진'이란다.

## 1. 🏗️ 오디오 엔진의 두 심장: TTS vs MP3

우리 시스템은 상황에 따라 두 가지 다른 기술을 사용해.

| 구분 | **Web Speech API (TTS)** | **HTML5 Audio (MP3/Cloud)** |
| :--- | :--- | :--- |
| **작동 원리** | 브라우저 내장 음성 합성 엔진 사용 | 외부 오디오 파일(.mp3, .wav) 재생 |
| **장점** | 데이터 소모 없음, 즉각적인 텍스트 변환 | 고품질 성우 목소리, 세밀한 감정 표현 |
| **단점** | 기계적인 어조, 브라우저 환경에 민감함 | 네트워크 비용 발생, 로딩 시간 필요 |
| **핵심 객체** | `window.speechSynthesis` | `HTMLAudioElement (new Audio())` |

### 🔄 하이브리드 전략
코드 내의 `playAuto()` 메서드를 보면, 먼저 MP3가 있는지 확인하고(`playMP3`), 없다면 폴백(Fallback)으로 실시간 TTS(`playText`)를 실행하는 똑똑한 구조를 가지고 있어.

---

## 2. 🔁 재생 위치 유지(Resume)의 기술적 비밀

단순히 `play()`와 `pause()`만 있는 게 아니야. 우리 코드의 `resume()` 메서드는 **4단계 안정화 시퀀스**를 거쳐.

### 🛠️ 4단계 안정화 시퀀스
1.  **AudioContext 활성화**: 브라우저의 전력 절약 모드로 인해 잠든 오디오 엔진을 깨워.
2.  **TTS/MP3 개별 재개**: 각각의 엔진(`synthesis.resume()`, `audioElement.play()`)에 명령을 내려.
3.  **Watchdog(감시견) 작동**: 300ms 후에 실제로 소리가 나는지 검사해.
4.  **강제 킥스타트**: 만약 재생되지 않고 있다면 현재 문장부터 다시 시작하도록 강제 집행해!

---

## 3. 🚑 'Hard Reset' 전략: 좀비 상태 탈출법

브라우저의 TTS 엔진(`SpeechSynthesis`)은 가끔 멈춘 상태에서 명령을 듣지 않는 '좀비 상태'가 되곤 해. 이때 사용하는 필살기가 바로 **Hard Reset**이야.

```typescript
// [Fix] TTS 좀비 체크 및 Hard Reset 로직 발췌
if (this.synthesis.paused || !this.synthesis.speaking) {
  console.log('[AudioService] 🚑 TTS stalled → Hard Reset');
  this.synthesis.cancel(); // 1. 일단 모든 명령을 취소하고
  setTimeout(() => {
    this.speakCurrentSentence(); // 2. 현재 문장부터 다시 읽게 함
  }, 50);
}
```

이 기술 덕분에 사용자는 오디오가 멈췄을 때 앱을 껐다 켤 필요 없이, 시스템이 스스로 문제를 인지하고 치료하는 경험을 하게 되는 거란다.

---

## 💡 학생들을 위한 TIP
> `unlockAudio()` 메서드를 눈여겨보렴! 
> 모바일 브라우저는 사용자의 첫 번째 터치가 있기 전까진 소리를 내지 못하게 막아둬. 이를 해결하기 위해 **0.1초짜리 아주 작은 소리**를 먼저 재생해서 엔진의 잠금을 푸는 기술이 적용되어 있단다. 실무에서 아주 요긴하게 쓰이는 '꼼수'이자 '정석'이지!
