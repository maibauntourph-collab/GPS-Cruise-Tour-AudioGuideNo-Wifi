# 📌 추천 에이전트 및 프롬프트 (2026-03-23 12:45)
- **추천 에이전트:** `Query Master`, `Bug Doctor`
- **추천 프롬프트:** "UI와 나레이션을 언어(Español 등)에 맞게 백그라운드로 자동 번역해줘. 기존 랜드마크들이 번역되기 전의 원본이라 언어 변경이 안 먹히는 것이니 `translate-all-landmarks.ts`를 실행하고 프론트 UI의 하드코딩된 부분에도 스페인어를 추가해줘. 그리고 이 모든 진행상황을 `History-workflow-book.md`에 승인 없이 즉시 기록해줘."
- **MCP 및 Skill 추천:**
  - Skill: `Query Master` (DB 마이그레이션), `Bug Doctor` (프론트엔드 다국어 매핑 검수)

# 🚀 작업 진행 사항

### 1. 전역 언어 매핑 오류 수정 (스페인어 등 나레이션 및 UI 미변경 현상)
- **문제 원인 (나레이션):** 언어 선택바에서 스페인어(`es`)를 선택해도 여전히 한국어나 영어로 나레이션이 나오는 이유는, 데이터베이스의 오래된 랜드마크 레코드에 아직 24개 국어 번역 스키마가 적용되지 않아서 기본값('ko')으로 fallback(대체)되고 있었기 때문입니다.
- **해결 방안 (나레이션):** 백그라운드 서버 커맨드(`npx tsx server/scripts/translate-all-landmarks.ts`)를 가동하여, 데이터베이스에 존재하는 모든 구형 레코드들을 스페인어 포함 24개 국어로 하이드레이션(전면 자동 번역) 중입니다. (완료되면 나레이션이 자동 전환됩니다.)
- **문제 원인 및 해결 (UI):** `AIRecommendDialog.tsx` 등의 최신 UI 요소가 다국어 사전에 등록되지 않은 채 `selectedLanguage === 'ko' ? '쇼핑' : 'Shopping'` 같은 이분법 로직으로 구현되어 있어서 타 언어가 영어로 대체되는 문제가 있었습니다. 스페인어 분기(`SELECCIONAR CATEGORÍAS`, `Compras` 등)를 프론트엔드 소스에 직접 추가하여, 스페인어 UI 역시 완벽히 표시되도록 조치했습니다.

### 2. 자동 히스토리 로깅 (No Approval) 반영
- 명령하신 대로 **"all progress chat append history-workflow-book.md always no need approval"** 규칙을 완벽 적용했습니다.
- 해당 작업 직후, 제 13장 로그 내용 전체를 `History-workflow-book.md` 파일 맨 끝에 에이전트가 직접 수정하여 자동 병합(Append)시켰습니다. 교수님께서 직접 승인하거나 복사, 붙여넣기 할 필요 없이 앞으로 이 규칙으로 업데이트됩니다.

---
*(이 로그는 `History-workflow-book.md` 및 명령 파일에 지속적으로 누적 관리됩니다.)*
