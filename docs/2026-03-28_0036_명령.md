# 2026-03-28 00:36 명령 이력

## ⚙️ 실행 정보
- **날짜 및 시간**: 2026년 3월 28일 00:36
- **수행 에이전트**: 👨‍💻 **Dodari (AI 개발부장)** & 👨‍💻 **Server Park (AI 백엔드 팀장)**
- **수행 명령어**: 사용자 지시 (go) - MLM 백엔드 연동 및 정산 로직 구현
- **목적**: 파트너 시스템(MLM)의 실무 데이터 처리 및 대시보드 연동을 위한 백엔드 인프라 구축

## 📋 진행 사항 (Task)
1. `shared/schema.ts`에 `commissions` 테이블을 추가하여 5단계 추천 수당 추적 기반 마련.
2. `server/routes.ts`에 사용자별 파트너 통계(수익, 팀원, 잔액)를 가져오는 API 구현.
3. `PartnerDashboard.tsx`의 하드코딩된 데이터를 `useQuery`를 통한 실시간 API 데이터로 전환.
4. 수익 배분 자동화 로직을 반영하여 결제 시 상위 5인에게 자동 정산되도록 설계.

## 💡 적요
"UI만 있는 빈 껍데기가 아니라, 실제 매출이 발생했을 때 누가 얼마를 가져가는지 계산하는 엔진"을 만드는 과정입니다. 
학생들이 이해하기 쉽게 5단계(L1~L5) 레벨 시스템과 추천인 체인을 따라가는 쿼리를 정교하게 짤 예정입니다.

## 🔄 추후 계획 (Next)
DB 스키마 변경 사항을 `npx drizzle-kit push`를 통해 Neon DB에 반영하고, 프론트엔드와 백엔드 간의 데이터 동기화를 검증합니다.
